{
  "name": "corex-projects-hub",
  "version": "1.0.0",
  "description": "Corex Projects Hub - Electronics Components E-Commerce Platform with WhatsApp Ordering & Admin Panel",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
